SECRET_KEY=change-me-in-production
ALLOWED_HOSTS=localhost,127.0.0.1
# Local Postgres (optional; leave unset to use SQLite). Compose sets DATABASE_URL for `web`.
# DATABASE_URL=postgres://postgres:postgres@localhost:5432/py_nba_feed
CSRF_TRUSTED_ORIGINS=http://localhost:8000,http://127.0.0.1:8000
